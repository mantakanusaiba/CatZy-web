@model Catzy.Models.Product
@{
    ViewBag.Title = "Catzy - Update Product";
}


<nav class="navbar">
    <div class="logo">CatZy</div>
    <ul class="nav-links">
        <li><a href="#" class="active">Product Management</a></li>
        <li><a href="#">Adoption Listing</a></li>
        <li><a href="#">Doctor Approval</a></li>
        <li><a href="@Url.Action("Index", "Product")" class="btn-back">Back</a></li>
    </ul>
</nav>


<div class="admin-container">


    <aside class="sidebar">
        <ul>
            <li><a href="@Url.Action("Index", "Product")">📂 All Products</a></li>
            <li><a href="@Url.Action("Add", "Product")">➕ Add Product</a></li>
            <li><a href="@Url.Action("Category", "Product", new { category = "Pet Food" })">🥩 Pet Food</a></li>
            <li><a href="@Url.Action("Category", "Product", new { category = "Toys" })">🧸 Toys</a></li>
            <li><a href="@Url.Action("Category", "Product", new { category = "Health Care" })">❤️ Health Care</a></li>
            <li><a href="@Url.Action("Category", "Product", new { category = "Accessories" })">⭐ Accessories</a></li>
        </ul>
    </aside>


    <main class="content">
        <h1>Update Product</h1>
        <p>Modify the details below to update this product.</p>

        @using (Html.BeginForm())

        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(m => m.Id)

            <div class="form-grid">
                <div class="form-group">
                    @Html.LabelFor(m => m.Name, "Product Name")
                    @Html.TextBoxFor(m => m.Name, new { @class = "input" })
                </div>

                <div class="form-group">
                    @Html.LabelFor(m => m.Price, "Price (Tk)")
                    @Html.TextBoxFor(m => m.Price, new { @class = "input" })
                </div>

                <div class="form-group">
                    @Html.LabelFor(m => m.Category, "Category")
                    @Html.TextBoxFor(m => m.Category, new { @class = "input" })
                </div>

                <div class="form-group">
                    @Html.LabelFor(m => m.Stock, "Stock Quantity")
                    @Html.TextBoxFor(m => m.Stock, new { @class = "input" })
                </div>

                <div class="form-group full-width">
                    @Html.LabelFor(m => m.Description, "Description")
                    @Html.TextAreaFor(m => m.Description, new { @class = "textarea" })
                </div>

                <div class="form-group full-width">
                    @Html.LabelFor(m => m.Icon, "Icon")
                    @Html.TextBoxFor(m => m.Icon, new { @class = "input" })
                </div>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn">✏️ Update Product</button>
            </div>
        }
    </main>
</div>

<style>
    body {
        margin: 0;
        font-family: 'Comic Sans MS', cursive, sans-serif;
        background-color: #f9d097;
    }

    .navbar {
        background-color: #644426;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 14px 40px;
        width: 100%;
        position: fixed;
        top: 0;
        left: 0;
        z-index: 1000;
    }

        .navbar .logo {
            font-size: 28px;
            font-weight: bold;
            color: white;
        }

    .nav-links {
        list-style: none;
        display: flex;
        gap: 25px;
        margin: 0;
        padding: 0;
    }

        .nav-links a {
            color: white;
            text-decoration: none;
            font-weight: bold;
            transition: 0.2s;
        }

            .nav-links a:hover, .nav-links a.active {
                color: #f9d097;
            }

    .admin-container {
        display: flex;
        min-height: 100vh;
    }

    .sidebar {
        width: 260px;
        background-color: #fff1c1;
        padding: 25px;
        border-right: 2px solid #e5c185;
        position: fixed;
        top: 70px;
        left: 0;
        bottom: 0;
        overflow-y: auto;
        margin-top: 0;
    }

        .sidebar ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

            .sidebar ul li {
                margin-bottom: 15px;
            }

                .sidebar ul li a {
                    text-decoration: none;
                    display: block;
                    color: #6b4b2e;
                    font-weight: bold;
                    padding: 12px 15px;
                    border-radius: 5px;
                    border: 2px solid #e5c185;
                    transition: 0.3s;
                }

                    .sidebar ul li a.active, .sidebar ul li a:hover {
                        background-color: #e5c185;
                    }

    .content {
        flex-grow: 1;
        padding: 30px;
        margin-left: 230px;
        margin-top: 80px;
    }

        .content h1 {
            color: #6b4b2e;
            font-size: 26px;
            margin-bottom: 10px;
        }

        .content p {
            color: #8a5e3c;
            font-size: 14px;
            margin-bottom: 20px;
        }


    .form-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 20px;
    }

    .form-group {
        display: flex;
        flex-direction: column;
    }

        .form-group label {
            font-weight: bold;
            color: #6b4b2e;
            margin-bottom: 5px;
        }

        .form-group .input, .form-group .textarea {
            padding: 10px;
            border: 2px solid #e5c185;
            border-radius: 8px;
            background: #fffbe9;
            font-size: 14px;
        }

        .form-group.full-width {
            grid-column: span 2;
        }

        .form-group .textarea {
            height: 100px;
            resize: none;
        }

    .form-actions {
        margin-top: 25px;
        display: flex;
        gap: 15px;
    }

    .btn {
        background-color: #e5c185;
        padding: 10px 16px;
        border-radius: 12px;
        text-decoration: none;
        color: #6b4b2e;
        font-weight: bold;
        border: none;
        cursor: pointer;
    }

        .btn:hover {
            background-color: #dab078;
        }
</style>
